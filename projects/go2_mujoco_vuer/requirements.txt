# MuJoCo + Go2/G1 Simulation Web Interface
# requirements.txt

# Core
mujoco>=3.0.0
numpy>=1.24.0

# Web server
fastapi>=0.100.0
uvicorn>=0.23.0
websockets>=11.0

# Optional: Unitree SDK (for DDS bridge)
# unitree_sdk2py  # Install separately if needed

# Visualization
# Three.js loaded via CDN (no pip install needed)
